
#user  nobody;
worker_processes  auto;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


#commented out http header, include, default_type, sendfile and keepalive_timeout
http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

   server {
   
        listen  80;
        server_name www.quantumsafenginx.com;
        return 301 https://quantumsafenginx.com$request_uri;
  

}

   server {

#    if ($host = www.quantumsafenginx.com) {
 #       return 301 https://$host$request_uri;
  #  } # managed by Certbot


	#used to say listen 80 default_server; 
        listen  80; 
        server_name  quantumsafenginx.com;
	return 301 https://quantumsafenginx.com$request_uri;
        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   html;
            #index  index.html index.htm;
	     index daddy.html index.htm;
        }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
 

    

}


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

  # server {
   #     listen  443 ssl;
    #    server_name quantumsafenginx.com;
    #    return 301 https://www.quantumsafenginx.com$request_uri;
 # }

    # HTTPS server
    #
    server {
        listen       443 ssl;
         server_name  www.quantumsafenginx.com quantumsafenginx.com;
	
	#SSL STUFF
#	ssl on; 
    ssl_certificate /etc/letsencrypt/live/www.quantumsafenginx.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/www.quantumsafenginx.com/privkey.pem; # managed by Certbot
#	ssl_certificate /home/ibra/nginx_pki/non_pqs/test.crt; 
#	ssl_certificate_key /home/ibra/nginx_pki/non_pqs/test.key;

   #     ssl_session_cache    shared:SSL:1m;
   #     ssl_session_timeout  5m;
	
	ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;

        ssl_protocols TLSv1.3;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
        #ssl_prefer_server_ciphers  on;
	#ssl_ecdh_curve 'kyber512:kyber768:kyber1024:p256_kyber90s512:p256_ntru_hps2048509';
        # ssl_ecdh_curve 'kyber512';
	#ssl_ecdh_curve auto; <-- instructs nginx to use a list built into the OpenSSL library http://nginx.org/en/docs/http/ngx_http_ssl_module.html#ssl_ecdh_curve
        location / {
            root   html;
            index  daddy.html index.htm;
        }
    


}

 # HTTPS server 2
    #
    server {
        listen       6001 ssl;
         server_name  www.quantumsafenginx.com;
        #server_name oqs-https-server;

        #SSL STUFF
#       ssl on;
 #   ssl_certificate /etc/letsencrypt/live/www.quantumsafenginx.com/fullchain.pem; # managed by Certbot
  #  ssl_certificate_key /etc/letsencrypt/live/www.quantumsafenginx.com/privkey.pem; # managed by Certbot
      ssl_certificate /home/ibra/nginx_pki_new/pki/server.crt;
       ssl_certificate_key /home/ibra/nginx_pki_new/pki/server.key;

   #     ssl_session_cache    shared:SSL:1m;
   #     ssl_session_timeout  5m;

        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;

        ssl_protocols TLSv1.3;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
        #ssl_prefer_server_ciphers  on;
       ssl_ecdh_curve 'kyber512:kyber768:kyber1024:p256_kyber90s512:p256_ntru_hps2048509';
        # ssl_ecdh_curve 'kyber512';
        #ssl_ecdh_curve auto; <-- instructs nginx to use a list built into the OpenSSL library http://nginx.org>       
	 location / {
            root   html;
            index  daddy.html index.htm;
        }
 
   }

}
